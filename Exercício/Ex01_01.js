const prompt = require("prompt-sync")();

let not1 = Number(prompt('qual é o primeiro número?'));
let not2 = Number(prompt('qual é o segundo número?')); 
let not_Final = not1 + not2 
console.log ("A soma dos dois números é de :" + not_Final);
